import React from 'react';

const Category = (props) => {
  const style = {
    backgroundColor: props.color,
  };
  return (
    <div style={style}>
      
    </div>
  );
};

export default Category;